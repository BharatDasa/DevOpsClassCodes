apiVersion: v1
kind: Service
metadata:
  name: addressbook
spec:
  selector:
    app: addressbook
  ports:
    - port: 80
      targetPort: 8080



apiVersion: v1
kind: Service
metadata:
  name: addressbook
  namespace: infra
  labels:
    app: addressbook

spec:
  type: ClusterIP

  selector:
    app: addressbook

  ports:
    - name: http
      port: 80
      targetPort: 8080
