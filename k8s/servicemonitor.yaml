apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: addressbook
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: addressbook
  namespaceSelector:
    matchNames:
    - addressbook
  endpoints:
  - port: http
    interval: 15s
